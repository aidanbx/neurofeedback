Implemented spectral history fixes requested by the user. Spectrogram mode now defaults to log_power, the default spectrogram window is 120s, selectable windows are 30/60/120/180s, and retained PSD history is capped at 180s. Fixed the freeze caused by pruning at stable length by adding an explicit historyVersion dependency for binned PSD recomputation. Verification: npm run build:frontend passed. Staged worktree cleanup for commit/push.